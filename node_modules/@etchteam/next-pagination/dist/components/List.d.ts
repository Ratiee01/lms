import React from 'react';
interface ListProps {
    children: React.ReactNode;
    theme: {
        [key: string]: any;
    };
}
export default function List({ children, theme }: ListProps): JSX.Element;
export {};
